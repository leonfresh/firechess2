# Discord test setup — 6 September 2026

Application: Chaos Chess (`1546003954616500245`).

Activities enabled, default Entry Point created, maximum participants 2, Web
platform enabled. User Install and Guild Install enabled with applications.commands.

Saved URL mappings:

| Prefix | Target |
| --- | --- |
| `/` | `protest-nutten-liz-bill.trycloudflare.com` |
| `/party` | `neo-warrant-meyer-impose.trycloudflare.com` |
| `/jsdelivr` | `cdn.jsdelivr.net` |
| `/chess-images` | `images.chesscomfiles.com` |

These are temporary Cloudflare quick tunnels, served by this computer. They stop
working when their processes stop. New quick tunnels need updated mappings and a
new NEXT_PUBLIC_PARTYKIT_HOST followed by rebuilding the Activity.

The Application ID and current multiplayer hostname are saved in `.env.local`.
Local services: Activity 3001 (production build), FireChess backend 3002,
PartyKit 1999. Cloudflared executable installed under
`%LOCALAPPDATA%/ChaosChess/tools/cloudflared.exe`.

Verified: production build passed, public Activity URL returned 200, two clients
exchanged legal moves through the public multiplayer tunnel.

Actual Discord launch verified in the FireChess server's chaos-chess text channel.
The SDK handshake completed, the lobby and board rendered, Stockfish played Nf3,
and a player move e7-e5 was accepted in the embedded board.
Full multiplayer matches, later modifier drafts, reconnects, and sound playback
still need in-Discord verification. Public discovery is not enabled; this is a
development test setup, not permanent production hosting.

## Presentation pass

The Activity now supplies its own friend/practice lobby and modifier draft through
`components/chaos-presentation.tsx`. The shared game's default presentation remains
available outside the Activity. Four approved toy illustrations are mapped to cards;
other modifier cards currently use styled chess glyphs. The anomaly picker and
in-game modifier icons still use the original artwork.

Original synthesized sound cues are generated by `scripts/make-audio.mjs` during
asset preparation. First-run volume is 35%, with meme sounds disabled. A header
volume control replaces the old settings panel.

Browser verification: started White practice, skipped the anomaly, played through
the first draft, selected and confirmed King's Chains, and verified the modifier
was applied and AI play resumed. Checked the lobby and board at 390px width and
verified the mute control. Type checking and production compilation passed.
Listening verification, full deck art, anomaly redesign, automatic Discord room
pairing, and comprehensive AI/multiplayer QA remain future work.

## Toy arena and animated draft follow-up

The modifier draft now consumes the shared game's actual reveal state: cards deal
face-down, flip in sequence, lift on selection, and animate out on confirmation.
Artwork fills a consistent frame; desktop frames preserve the whole square image.
Reduced-motion preferences disable the new animations.

Added Camel, Dragon Rook and Pawn Bayonet illustrations, bringing the illustrated
card set to seven. Other cards now use original vector pieces, not font glyphs.
`scripts/make-pieces.mjs` generates both colors of all six standard pieces and all
17 special forms referenced by the shared renderer. Existing modifier assignment,
cooldown badges and effect logic are retained. The Activity uses this set regardless
of the website's selected piece theme; special capture animations also use it.

The arena has a raised board, cream/teal squares, a compact power stash with player
and opponent tabs, expandable descriptions, and collapsible match history. The
normal website presentation remains available outside the Activity.

Verified in browser: played through turn five, observed the face-down deal and
revealed cards, selected Dragon Rook, confirmed the pick, verified both rooks used
`wDr.svg`, and observed AI play resume. Checked draft layout at 390 × 844 and
desktop. All 34 special-piece asset references resolved to generated files.
The original anomaly selection screen and some secondary effect badges remain;
this pass does not claim to fix the game's deeper AI or multiplayer behavior.

## Branding

Original vector rook-and-lightning logo: assets/logo.svg. App icon SVG and 1024px
PNG plus 1024x576 Activity cover/background are generated by scripts/make-brand.mjs.
The game header and favicon use the new branding. Discord General Information app
icon and Activities > Art Assets background/cover were uploaded and saved in the
Developer Portal. Video preview remains empty. Discord warns that asset caching
may delay the visible update.

## Lobby and progression update
- Activity lobby exposes existing quick matchmaking plus public queue-room list (5-second refresh; private friend rooms excluded).
- Join endpoint atomically claims an empty seat; concurrent-join integration check passed.
- Activity anomaly picker uses animated toy cards; all four choices and draft cards available without progression locks. Consumed-power exclusions preserved.
- Website progression unchanged. Activity hides website chat/login prompts; use Discord chat.
- Verified: shared website and Activity production builds, room listing/concurrent join, outer anomaly selection into AI gameplay. Changes local, not pushed.

